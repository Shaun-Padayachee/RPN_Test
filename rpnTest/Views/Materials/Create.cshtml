@model rpnTest.Models.Material

@{
    ViewBag.Title = "Create";
}

<h2>Create</h2>


@using (Html.BeginForm()) 
{
    @Html.AntiForgeryToken()
    
    <div class="form-horizontal">
        <h4>Material</h4>
        <hr />
        @Html.ValidationSummary(true)

        <div class="form-group">
            @Html.LabelFor(model => model.mat_desc, new { @class = "control-label col-md-2" })
            <div class="col-md-10">
                @Html.EditorFor(model => model.mat_desc)
                @Html.ValidationMessageFor(model => model.mat_desc)
            </div>
        </div>

        <div class="form-group">
            @Html.LabelFor(model => model.mat_cost_price, new { @class = "control-label col-md-2" })
            <div class="col-md-10">
                @Html.EditorFor(model => model.mat_cost_price)
                @Html.ValidationMessageFor(model => model.mat_cost_price)
            </div>
        </div>

        <div class="form-group">
            @Html.LabelFor(model => model.mat_mu_price, new { @class = "control-label col-md-2" })
            <div class="col-md-10">
                @Html.EditorFor(model => model.mat_mu_price)
                @Html.ValidationMessageFor(model => model.mat_mu_price)
            </div>
        </div>

        <div class="form-group">
            <div class="col-md-offset-2 col-md-10">
                <input type="submit" value="Create" class="btn btn-default" />
            </div>
        </div>
    </div>
}

<div>
    @Html.ActionLink("Back to List", "Index")
</div>

@section Scripts {
    @Scripts.Render("~/bundles/jqueryval")
}
